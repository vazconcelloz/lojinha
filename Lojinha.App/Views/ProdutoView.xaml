<UserControl x:Class="Lojinha.App.Views.ProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBox Width="200" Margin="0,0,8,4" Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" ToolTip="Nome" />
            <TextBox Width="150" Margin="0,0,8,4" Text="{Binding CodigoBarras, UpdateSourceTrigger=PropertyChanged}" ToolTip="Código de barras" />
            <ComboBox Width="150" Margin="0,0,8,4" ItemsSource="{Binding Categorias}" DisplayMemberPath="Nome"
                      SelectedItem="{Binding CategoriaSelecionada}" ToolTip="Categoria" />
            <ComboBox Width="110" Margin="0,0,8,4" ItemsSource="{Binding TiposVenda}"
                      SelectedItem="{Binding TipoVendaSelecionado}" ToolTip="Tipo de venda" />
        </WrapPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBox Width="110" Margin="0,0,8,4" Text="{Binding PrecoCusto, UpdateSourceTrigger=PropertyChanged}" ToolTip="Preço de custo" />
            <TextBox Width="110" Margin="0,0,8,4" Text="{Binding PrecoVenda, UpdateSourceTrigger=PropertyChanged}" ToolTip="Preço de venda" />
            <TextBox Width="110" Margin="0,0,8,4" Text="{Binding EstoqueMinimo, UpdateSourceTrigger=PropertyChanged}" ToolTip="Estoque mínimo" />
            <Button Content="Adicionar" Padding="12,4" Command="{Binding AdicionarCommand}" />
        </WrapPanel>

        <TextBlock Grid.Row="2" Text="{Binding MensagemErro}" Foreground="Red" Margin="0,0,0,8" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox Width="260" Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <DataGrid Grid.Row="4" ItemsSource="{Binding Produtos}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" />
                <DataGridTextColumn Header="Código" Binding="{Binding CodigoBarras}" Width="120" />
                <DataGridTextColumn Header="Categoria" Binding="{Binding Category.Nome}" Width="120" />
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoVenda}" Width="80" />
                <DataGridTextColumn Header="Custo" Binding="{Binding PrecoCusto}" Width="80" />
                <DataGridTextColumn Header="Venda" Binding="{Binding PrecoVenda}" Width="80" />
                <DataGridTextColumn Header="Est. Mín." Binding="{Binding EstoqueMinimo}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
