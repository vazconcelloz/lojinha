<UserControl x:Class="Lojinha.App.Views.FornecedorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Width="200" Text="{Binding NovoNome, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Nome" />
            <TextBox Width="200" Margin="8,0,0,0" Text="{Binding NovoContato, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Contato" />
            <Button Content="Adicionar" Margin="8,0,0,0" Padding="12,4" Command="{Binding AdicionarCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding MensagemErro}" Foreground="Red" Margin="0,0,0,8" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Fornecedores}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*" />
                <DataGridTextColumn Header="Contato" Binding="{Binding Contato}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
